# دليل العمل — mfsoccer على جهازين (ويندوز + ماك)

المشروع محفوظ على **GitHub**، وهو سحابتك الدائمة التي **لا تنام**.
الجهازان **مستقلّان**: ويندوز مطفي؟ افتح الماك واشتغل عادي، والعكس.
لا اعتماد بين الجهازين — **GitHub** هو الذي يزامن كل شيء.

المستودع: https://github.com/coachmf/mfsoccer

---

## ① إعداد الماك (مرة واحدة فقط)

افتح **Terminal** على الماك، والصق الأوامر سطراً سطراً:

**1) انسخ المشروع** (خارج OneDrive — مهم):
```
cd ~/Desktop
git clone https://github.com/coachmf/mfsoccer.git
cd mfsoccer
```

**2) فعّل صلاحية الرفع** (‏Homebrew مثبّت عندك):
```
brew install gh
gh auth login
```
اختر: **GitHub.com** ← **HTTPS** ← **Login with a web browser** ← اتبع الرابط ووافق.

**3) افتح Claude Code** على الماك في مجلد `mfsoccer` (تبويب Code ← افتح المجلد).

---

## ② الروتين اليومي (على أي جهاز)

**قبل ما تبدأ العمل** — اسحب أحدث نسخة:
```
git pull
```

**بعد ما تخلّص** — ارفع تغييراتك:
```
git add -A
git commit -m "وصف مختصر للتغيير"
git push
```

> 💡 يكفي أن تقول لـ Claude **«ارفع»** ويعملها تلقائياً.

**القاعدة الذهبية:** اسحب (pull) قبل، وارفع (push) بعد — فلا يتعارض الجهازان أبداً.

---

## ③ ملاحظات مهمة

- ⚠️ **لا تعتمد على OneDrive** لهذا المشروع بين الجهازين — مزامنة OneDrive لمجلد `.git` تسبّب تلفاً. **GitHub** وحده هو المزامنة. لذلك انسخ المشروع على الماك **خارج** OneDrive.
- 💬 **المحادثة مع Claude** مستقلة لكل جهاز — على الماك تبدأ محادثة جديدة، لكن **الكود مشترك** عبر git فتكمل عملك بلا مشاكل.
- ✅ نسخة الموقع الحيّة على **Netlify** تتحدّث تلقائياً عند كل `push` — من أي جهاز.

---

## ④ أوامر سريعة

| ماذا تريد | الأمر |
|---|---|
| اسحب آخر تحديث | `git pull` |
| شوف التغييرات | `git status` |
| احفظ وارفع | `git add -A && git commit -m "..." && git push` |
| شوف آخر الالتزامات | `git log --oneline -5` |

---

*آخر تحديث: 2026-09-09*
